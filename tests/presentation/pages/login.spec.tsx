import React from 'react'
import { Login } from '@/presentation/pages'

import { render } from '@testing-library/react'

describe('Login Page', () => {
  test('', async () => {
    render(<Login />)
  })
})
